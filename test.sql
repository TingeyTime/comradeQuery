/*
    @name The Query
    @author Nate Tingey

This is my Description
*/

SELECT
    *
    -- a comment
FROM
    everything.everwhere AS ee
WHERE
    ee.all is 'good';


/*
    @name The Second Query
    @author Someone Else
    @db theDatabase

This is another Description
*/

SELECT 'something'
FROM everything.everwhere AS ee
WHERE ee.all is 'bad';
